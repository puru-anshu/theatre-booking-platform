server:
  port: 8082

spring:
  application:
    name: show-service

  datasource:
    url: jdbc:postgresql://postgres:5432/movie_db
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
